{{ render 'editable_bite' model }}
<div class='extra_site_controls'>

    <button {{ bind-attr class='isDirty::disabled :btn :btn-lg :btn-primary'}} {{action "submitAction" model}}>
        <i class='icon-save'></i>
        {{t 'button.save'}}
    </button>
</div>
